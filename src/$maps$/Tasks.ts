//
export const tasks = [
    {
        id: "#settings",
        component: ()=>import("../$solid$/settings/Settings"),
        icon: "settings",
        active: false
    }
];

//
export default tasks;
